{
  "name": "@s-agent/analyzer",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc -b --pretty false",
    "test": "vitest run"
  },
  "dependencies": {
    "@s-agent/parser": "workspace:*",
    "@s-agent/rules": "workspace:*",
    "@s-agent/shared": "workspace:*"
  }
}
