{
  "name": "@s-agent/core",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc -b --pretty false",
    "test": "vitest run"
  },
  "dependencies": {
    "@s-agent/analyzer": "workspace:*",
    "@s-agent/explainer": "workspace:*",
    "@s-agent/rules": "workspace:*",
    "@s-agent/verifier": "workspace:*"
  }
}
